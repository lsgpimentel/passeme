<li class="dropdown-submenu">
<a tabindex="-1" href="#"><%= timetable.name %><%= t('.active') if timetable.active? %></a>
  <ul class="dropdown-menu">
    <li>
      <%= link_to t('.event_sources'), calendar_event_sources_path(timetable.calendar) %>
    </li>
    <% unless timetable.active? %>
      <li>
        <%= link_to t('.make_active'), timetable_make_active_path(timetable), class: "activate-timetable", method: :put %>
      </li>
    <% end %>
  </ul>
</li>

